---
name: wgx-guard

"on":
  push:
    paths: [".wgx/**", ".github/workflows/wgx-guard.yml", "pyproject.toml", "Cargo.toml"]
  pull_request:
    paths: [".wgx/**", ".github/workflows/wgx-guard.yml", "pyproject.toml", "Cargo.toml"]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  guard:
    # Fleet-Repos, die von metarepo aus via wgx up Templates beziehen,
    # werden auf den kanonischen Guard-Workflow aus heimgewebe/wgx gesetzt.
    uses: heimgewebe/wgx/.github/workflows/wgx-guard.yml@d8f5aa2730a55d059c1f41acd2eb029a3103cca5  # WGX PR #639 merge 2026-07-12
