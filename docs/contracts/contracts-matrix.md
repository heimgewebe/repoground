# RepoGround Contracts-Matrix

Diese Matrix dokumentiert die Verbindungen zwischen Systemrollen, Schemata, Abhängigkeiten und Drift-Risiken, wie sie aktuell in `merger/repoground/contracts/` abgebildet sind.

## Schema-Abhängigkeiten und Enum-Vollständigkeit

| Schema Name | Zweck | Typische Attribute | Verwendete Typen / Enumerationen | Referenzen (`$ref`) | Relevanz / Drift-Risiko |
| :--- | :--- | :--- | :--- | :--- | :--- |
| `bundle-manifest.v1.schema.json` | Kern-Manifest für RepoGround Artefakte. | `artifacts`, `role`, `sha256` | `artifactRole` (z.B. `canonical_md`, `chunk_index_jsonl`, `graph_index_json`) | - | Sehr hoch. Enum Drift zwischen Code und Schema führt zum Build-Ausfall. |
| `repoground-semantic-platforms.v1.schema.json` | Maschinenlesbare Grenze für den optionalen semantischen Dependency-Stack. | `status`, `default_enabled`, `unsupported_target_policy`, digest-gepinnter Compiler, exakt ein unterstütztes Target mit Input-/Constraints-/Lock-SHA-256, Root-Pins und Non-Claims | `status=optional_locked`; CPython 3.12/Linux x86-64/CPU-only; 58 ausgewählte Wheels; ein SHA-256 je Paket; andere Targets fail-closed | `repoground-release-candidate.v1.schema.json` bindet Plattformvertrag und Lockdateien an den Kandidatencommit | Hoch. Drift könnte einen fremden Wheel, eine andere Plattform oder eine stillschweigende Standardaktivierung vortäuschen. Ein valider Contract belegt weder semantische Qualität, Modellverfügbarkeit/-lizenz, Schwachstellenfreiheit, GPU-Support noch Default-Promotion. |
| `repobrief-latest-complete-registry.v2.schema.json` | Expliziter, monotoner Zeiger auf das neueste vollständig finalisierte RepoBrief-Bundle einer eindeutig gebundenen Quellenlane; v1 bleibt ausschließlich als kontrollierter Lese-/Migrationsinput bestehen. | `bundle.manifest_sha256`, `bundle.manifest_capture`, `source.repositories`, `selection.source_lane`, `publication`, `does_not_establish` | `version` const `v2`; Quellenidentität bereinigte Netzwerk-`repo_remote` oder Hash einer absoluten Root als `repo_root_sha256`; Auswahlbasis `generated_at_fail_closed_ties_v2`; Haltbarkeitsprotokoll `atomic_replace_with_durability_receipt_v2` einschließlich expliziter temporärer Mutation und Verzeichnisidentität | - | Sehr hoch. Drift könnte Manifestbytes und Hash entkoppeln, fremde gleichnamige Repositories kollidieren lassen oder einen nach `replace` unklaren Zustand fälschlich als nicht geschrieben bzw. dauerhaft bestätigt melden. Ein valider Contract beweist weder Inhalt, Remote-Aktualität noch Merge-Reife. |
| `query-result.v1.schema.json` | Formales Ranking- und Hit-Ergebnis. | `results`, `context_bundle`, `query_trace` | - | `query-context-bundle.v1.schema.json`, `range-ref.v1.schema.json` | Hoch. Basis für Agenten und Evaluierung. |
| `query-context-bundle.v1.schema.json` | Arbeitsmaterial für Hits (Hit + Evidence + Context). | `resolved_code_snippet`, `graph_context`, `provenance_type` | `provenance_type` (`explicit`, `derived`) | `range-ref.v1.schema.json` | Hoch. Kontext-Expansion (exact, window) muss mit Feldern matchen. |
| `range-ref.v1.schema.json` | Definiert präzise Quelltext-Bereiche. | `file`, `start_line`, `end_line`, `kind` | `kind` (`file`, `class`, `function`, `method`, etc.) | - | Mittel. Muss mit Extraktor-Outputs synchron bleiben. |
| `architecture.graph_index.v1.schema.json` | Graph-Signal für die Query Runtime. | `kind`, `version`, `distances`, `metrics` | `kind` (must be `lenskit.architecture.graph_index`) | - | Mittel. Distanzformate müssen von `query_core` verstanden werden. |
| `retrieval-eval.v1.schema.json` | Output von `eval_core` für Regression-Tracking. | `metrics`, `details`, `baseline`, `graph` | - | `query-result.v1.schema.json` (für `why`/`explain` Details) | Gering. Metriken sind stabil. |
| `review-retrieval-goldset.v1.schema.json` | Versionierter diagnostischer Input für review-nahe Retrieval-Regressionstests. | `query`, `category`, `expected_patterns`, `filters`, `accept_criteria` | Geschlossene Kategorie- und Filtervokabulare; `recall_at_10` als Verhältnis `0..1` | - | Mittel. Formale Gültigkeit und erwartete Muster beweisen weder Zielwahrheit noch allgemeine Retrieval-Qualität. |
| `pr-schau-delta.v1.schema.json` | Delta-Manifest für PR Reviews. | `commit_sha`, `diff_artifacts` | - | `pr-schau.v1.schema.json` | Mittel. Integrationstiefe in Agents prüfen. |
| `entrypoints.v1.schema.json` | Startpunkte im Graph für Boosts. | `entrypoints`, `weights` | - | - | Gering. |
| `python-symbol-index.v1.schema.json` | Deterministischer Python-Definitionsindex für exakte Symbolnavigation. | `symbols`, qualifizierter Name, Modul, Pfad, Zeilenbereich, Decorators | `kind` (`class`, `function`, `async_function`) | - | Hoch. Der Index ist `navigation_index`/`derived`, an `run_id` und kanonischen Dump-Hash gebunden und beweist weder Referenzen noch Laufzeitverhalten. |
| `python-call-graph.v1.schema.json` | Evidenzgestufte statische Python-Aufrufstellen und konservativ aufgelöste Symbolrelationen. | Caller-/Callee-Identität, Quellbereich, `relation_type`, `evidence_level`, `resolution_status`, Ziel-IDs, Aggregatzähler | `relation_type` (`calls`, `constructs`); `evidence_level` (`S0`, `S1`); `resolution_status` (`resolved`, `candidate`, `ambiguous`, `unresolved`) | Konsumenten verlangen kohärenten `python-symbol-index.v1` derselben Run-/Dump-Identität | Sehr hoch. Ein Drift zwischen Symbol-IDs, Scope-Auflösung, Aggregatzählern oder Bundle-Rollen könnte falsche Caller/Callee-Beziehungen erzeugen. S1 bedeutet genau ein statisch aufgelöstes lokales Ziel, nicht Call-Graph-Vollständigkeit oder Runtime-Reichweite. |
| `agent-query-session.v2.schema.json` | Provenienz-Wrapper für Agent Query Sessions (Runtime-Artefakt). | `session_authority`, `context_source`, `artifact_refs`, `claim_boundaries` | `session_authority` (const: `"agent_context_projection"`); `context_source` (enum: `projected`, `federated`, `mixed`, `unknown`); `artifact_refs` required: `query_trace_id`, `context_bundle_id`, `agent_query_session_id` (alle `string\|null`); `claim_boundaries` required: `proves`, `does_not_prove` | - | Hoch. `session_authority` und `claim_boundaries` definieren, was eine Session beweisen darf und was nicht. Drift hier würde falsche epistemische Grenzen erzeugen. |
| `federation-index.v1.schema.json` | Contract für den Föderations-Index (Liste verbundener Bundles). | `kind`, `version`, `federation_id`, `created_at`, `updated_at`, `bundles` | - | - | Hoch. Contract vorhanden und Runtime-implementiert (minimale Multi-Bundle-Aggregation); vollständige föderierte Ranking-Semantik noch offen. |
| `cross-repo-links.v1.schema.json` | Contract für Cross-Repo-Verknüpfungen zwischen Repository-Bundles. Root-Type: `array` (Liste von Link-Objekten). | `source_repo`, `target_repo`, `link_type`, `evidence_refs`, `confidence` | `link_type` (const: `co_occurrence`); `confidence` (const: `inferred`) | - | Hoch. Contract vorhanden (Root-Type korrigiert auf `array`); minimaler heuristischer Runtime-Producer `_build_cross_repo_links` implementiert: emittiert `co_occurrence`-Links mit `confidence: "inferred"` pro Repo-Paar in den finalen `results`; ganzes Artefakt-Array schema-validiert; CLI-Persistenz als `cross_repo_links.json` bei `--trace`. Bei `output_profile`-Projektion bleiben `cross_repo_links` im Wrapper mit `context_bundle` erhalten. **`co_occurrence` beweist ausschließlich gemeinsame Query-Präsenz — keine Identität, keine Abhängigkeit, keine semantische Gleichheit.** Offen: semantisch belastbare Identity-Engine. |
| `federation-conflicts.v1.schema.json` | Contract für föderierte Konfliktspuren (Array von Konfliktobjekten). | Item-Felder: `conflict_id`, `type`, `involved_results`, `description`, `resolution` | `type` (enum: `identity`, `path`, `symbol`, `evidence`, `provenance`); `resolution` (enum: `unresolved`) | - | Mittel bis hoch. Heuristisch/minimal emittiert (Runtime-Emission in `federation_query.py`, CLI-Persistenz in `cmd_federation.py`); semantische Konfliktlogik noch offen. |
| `citation-map.v1.schema.json` | Definiert Struktur einzelner Citation-Map-JSONL-Einträge (Belegadressen). | Required: `citation_id`, `repo_id`, `snapshot`, `canonical_range`; optional: `source_range`, `chunk_id`, `produced_by` | `source_range.status` (enum: `declared`, `exact`, `derived`, `unavailable`) | - | Hoch. Contract vorhanden, Manifest-Role `citation_map_jsonl` mit strengen Constraints in `bundle-manifest.v1` erlaubt, Producer offen. Konsumenten später: Query/Context/Agent Evidence Pack. Nicht-Ziel: Claim-Bewertung / Evidence Use. |
| `source-acquisition-report.v1.schema.json` | Contract für das rLens Source-Acquisition-Provenienz-/Diagnose-Artefakt (`remote_snapshot`-Jobs, geschrieben von `runner._write_source_acquisition_report`). Root-Type: `object`, `additionalProperties: false` an Root-, `summary`- und `repos[]`-Ebene. In Tests gegen geschriebene Reports validiert (`test_service_runner_pre_pull.py`). | Root: `schema`, `job_id`, `created_at`, `hub`, `mode`, `plan_only`, `remote_ref_policy`, `requested_remote_ref`, `summary`, `repos[]`; Repo-Item: `repo`, `original_path`, `scan_path`, `source_mode`, `status`, `remote_ref_policy`, `requested_remote_ref`, `resolved_ref`, `resolved_commit`, `remote_url_redacted`, `local_repo_mutated`, `message`, `stderr`, `warnings` | `schema` (const: `lenskit.source_acquisition_report.v1`); `mode`/`source_mode` (`remote_snapshot`); `status` (enum: `snapshot_created`, `planned`, `skipped`, `missing_remote`, `missing_ref`, `fetch_failed`, `archive_failed`, `extract_failed`, `error`); `local_repo_mutated` (`const: false` — vom Schema erzwungen, nicht nur konventionell) | - | Hoch. Provenienz-/Diagnose-Signal, **kein Wahrheitsbeweis**: belegt Struktur/Provenienz, nicht Byte-Identität zu lokal erzeugten Bundles. Redaction-Gates mit Tests reduzieren Credential-Leakage, garantieren es aber nicht absolut. |
| `federation-trace.v1.schema.json` | Contract ausschließlich für das **CLI-Dateiartefakt** `federation_trace.json` (geschrieben von `cmd_federation.py` bei `--trace`). Root-Type: `object`. `additionalProperties: false` an Root- und Bundle-Item-Ebene. **Dieses Schema beschreibt NICHT die inline API-/Runtime-Form** von `federation_trace`: Die Runtime-Form (aus `execute_federated_query` mit `trace=True`, weitergereicht durch `output_projection.py` in den Wrapper) trägt andere Felder (`queried_bundles_total`, `bundle_status`, `bundle_errors`, `bundle_traces`) und ist derzeit ohne eigenes JSON-Schema. Beide Formen heißen `federation_trace` — der Name ist identisch, die Shape ist verschieden. | CLI-Dateiartefakt-Felder: `query`, `timestamp`, `total_results`, `bundles[]`; Bundle-Item: `repo_id`, `bundle_path`, `status`, optional `fingerprint`, `latency_ms`, `error_message`. Runtime-Form (kein Schema): `queried_bundles_total`, `queried_bundles_effective`, `bundle_status` (dict), `bundle_errors`, `bundle_traces` | `status` (enum: `ok`, `missing`, `stale`, `error`, `filtered_out`, `bundle_path_unsupported`, `bundle_path_rejected`, `index_missing`, `query_error`) | - | Hoch. Schema validiert ausschließlich das CLI-Dateiartefakt `federation_trace.json`; Runtime-Inline-Trace wird erzeugt und durch die CLI in das Dateiartefakt projiziert/persistiert. Output-Profile-Projektion erhält die Runtime-Inline-Form im Wrapper (nicht das CLI-Dateiartefakt). **Beweist Ausführungs- und Aggregationsspur — keine semantische Identität, kein Ranking-Nachweis.** Offen: eigenes Schema für Runtime-Form, Latenz-Telemetrie, Status `missing` nicht emittiert. |
| `patch-evaluation.v1.schema.json` | Contract für ein **externes** Patch-Evaluation-Sidecar-Artefakt (`repobrief.patch_evaluation` v1). Root-Type: `object`, `additionalProperties: false` an Root- und verschachtelten Objekten. RepoBrief **konsumiert** es nur read-only (`core/patch_evaluation.py`, CLI `repobrief patch-evaluation validate`); RepoBrief erzeugt/führt/repariert es nicht und liest es nicht als Approval. Siehe [`patch-evaluation-v1.md`](patch-evaluation-v1.md) und [Agent Workbench Boundary](../architecture/repobrief-agent-workbench-boundary.md) (RBAW-V1-T002/T003). | Root: `kind`, `version`, `authority`, `producer`, `created_at`, `input`, `repobrief_context`, `workspace`, `patch`, `command_policy`, `commands_run`, `environment`, `status`, `does_not_establish`; Command-Item: `command`, `status`, optional `exit_code`/`log_ref`/`truncated` | `kind` (const `repobrief.patch_evaluation`); `authority` (const `external_evaluation_evidence`); `status` (enum: `passed`, `failed`, `mixed`, `error`, `incomplete`); Command-`status` (enum: `passed`, `failed`, `error`, `skipped`, `timeout`); `does_not_establish` (geschlossenes Vokabular; neun Pflicht-Non-Claims via `contains` erzwungen: `correctness`, `test_sufficiency`, `security_correctness`, `runtime_behavior_outside_evaluated_commands`, `merge_authorization`, `merge_readiness`, `regression_absence`, `repo_understood`, `claims_true`) | - | Hoch (Autoritätsgrenze). **Ein gültiges Artefakt ist begrenzte externe Evidenz, kein Approval.** `authority` ist auf `external_evaluation_evidence` gepinnt und wird beim Konsum nie hochgestuft; ein `status: passed` beweist weder Korrektheit noch Merge-Reife. Nicht-Ziel dieses Slice: der externe Sidecar selbst (RBAW-V1-T004). |

| `repoground-release-candidate.v1.schema.json` | Commit- und baumgebundenes Manifest für deterministische interne RepoGround-Quellkandidaten. Keine Veröffentlichungsfreigabe. | `project`, `source`, `license`, `archive`, `dependency_locks`, `semantic_extension`, `verification`, `does_not_establish` | `license.expression` const `LicenseRef-RepoGround-All-Rights-Reserved`; Distribution bleibt blockiert; vier Hash-Locks sind Pflicht; Semantik-Erweiterung ist explizit ausgeschlossen | - | Hoch. Drift könnte Byte-Reproduzierbarkeit vortäuschen oder einen internen Kandidaten fälschlich als öffentliche Freigabe erscheinen lassen. |
| `repobrief-status-truth.v1.schema.json` | Begrenzte Governance-Projektion für Task-/Board-Parität, Roadmap-Semantik, ausgewählte Claim-Freshness und Auditpaket-Reife. Keine zweite Taskautorität. | `authority`, `source_of_truth`, `status_semantics`, `task_summary`, `system_maturity`, `roadmap_surfaces`, `claim_freshness`, `audit_package_coverage`, `audit_packages`, `open_followups`, `health_semantics`, `does_not_establish` | `authority` const `governance_projection`; `product_readiness`/`release_readiness` bleiben explizit getrennt; Health-Pass darf nicht als Produkt-, Release-, Review-, Test-, semantische oder Runtime-Reife interpretiert werden | - | Hoch. Drift könnte begrenzte Taskabschlüsse zu System- oder Release-Reife überhöhen. Der Taskindex bleibt kanonisch; diese Projektion wird dagegen geprüft. |
| `answer-grounding-declaration.v1.schema.json` / `answer-grounding-verdict.v1.schema.json` | Contracts for declared answer grounding evidence and deterministic technical grounding results. | Declaration: `answer_id`, `snapshot_ref`, `used_citations`, `used_ranges`, `declared_non_claims`; Verdict: `status`, `citation_checks`, `range_checks`, `required_reading_checks`, `diagnostics`, caveats | Verdict `status` enum: `pass`, `warn`, `fail`, `degraded`, `not_applicable`; evidence check status enums are technical only | - | Hoch. A `pass` means only that checked citation/range/required-reading mechanics satisfied the contract; it does **not** establish answer truth, completeness, repo understanding, test sufficiency, merge readiness or security correctness. |

| `repobrief-ask-request.v1.schema.json` / `repobrief-ask-context-pack.v1.schema.json` | Contracts for the read-only Ask Frontdoor request and prepared context pack. | Request: `query`, `task_profile`, `token_budget`, `snapshot_policy`, `output_mode`; Context pack: `snapshot_ref`, `freshness`, `availability`, `required_reading`, `retrieval_hits`, `resolved_ranges`, `answer_scaffold` | `snapshot_policy.mode` is `existing_snapshot_only`; forbidden operation vocabulary includes implicit refresh, Git mutation, snapshot creation, patch/PR mutation, shell execution and merge authorization | - | Hoch. Defines an answer-context handoff boundary; valid packs do **not** prove actual reading, answer truth, completeness, runtime correctness, merge readiness or security correctness. |

## Canonical Pfade und Pflichtfelder

1.  **`canonical_md` in `bundle-manifest`:** Muss exakt ein Artefakt sein. Multi-Part Dumps deklarieren nur eines als kanonisch (`core/merge.py` via `resolve_canonical_md`).
2.  **`range_ref` vs. `derived_range_ref` in `query-result`:**
    *   `range_ref`: Darf nur gesetzt werden, wenn der `range_resolver` den Bereich im Bundle (`sqlite_index`) deterministisch gefunden hat.
    *   `derived_range_ref`: Source-backed Fallback.
    *   **Invariante:** Darf nie vermischt werden. Das `query-context-bundle` Schema macht dies transparent über `provenance_type`.
3.  **`explain` Block im `query-result`:**
    *   Strikter Vertrag: `additionalProperties: false`. Graph-Diagnostiken müssen in `why.diagnostics.graph` liegen, nie direkt in `why`.
    *   Muss exakt den Score widerspiegeln (`final_score` = `lexical` + `semantic` + `graph_bonus` + `penalties`).

## Drift-Schutz

- Ein **`Role-Completeness-Check`** (`test_role_completeness.py`) vergleicht `core/constants.py` (ArtifactRoles) mit `bundle-manifest.v1.schema.json`.
- Schemas werden im Build-Prozess und in Tests mit `jsonschema` validiert.

## Bekannte Lücken (Contract Matrix)

-   Formale Schemas für `cross_repo_links.json`, `federation_conflicts.json` und `federation_trace.json` sind vorhanden. `federation_trace.json` ist als CLI-Dateiartefakt stabilisiert: `additionalProperties: false` an Root- und Bundle-Item-Ebene, schema-validiert ausschließlich als CLI-Dateiartefakt. Die Runtime-Inline-Form (Shape: `queried_bundles_total`, `bundle_status`, ...) ist ein davon getrennter Trace, der durch die CLI in das Dateiartefakt transformiert und bei `--trace` persistiert wird. `federation_conflicts` ist heuristisch/minimal emittiert und per CLI-Trace persistierbar. `cross_repo_links` hat einen minimalen heuristischen Runtime-Producer (`_build_cross_repo_links`): emittiert `co_occurrence`-Links mit `confidence: "inferred"` pro Repo-Paar in den finalen `results`; ganzes Artefakt-Array schema-validiert; CLI-Persistenz als `cross_repo_links.json` bei `--trace`. **Epistemische Grenze: `co_occurrence` beweist ausschließlich gemeinsame Query-Präsenz; `federation_trace` beweist Ausführungs-/Aggregationsspur — keine Identität, keine Abhängigkeit, keine semantische Gleichheit.** Offen bleiben: semantisch belastbare Identity-Engine, durchgängige Nutzung in API-Endpoints, vollständige Integrationspfade, Latenz-Telemetrie.
-   `agent-query-session.v2.schema.json` ist eingetragen und belegt; offen bleiben: Lifecycle/Retention-Kontrakt, MCP-Binding-Schema.
