# Moved: RepoGround architecture

The current product architecture now lives at [`repoground.md`](repoground.md).

This file remains during RepoGround 3.x so historical links continue to resolve.
It is not a second product definition. Documents and machine contracts that use
`RepoBrief` as a versioned v1/v2 identifier retain their original meaning.
