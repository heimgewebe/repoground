# Moved: RepoGround build specification

The normative build/report specification now lives at
[`repoground-build-spec.md`](repoground-build-spec.md).

This compatibility file remains during RepoGround 3.x so old links do not fail.
It is not a second specification. Persisted identifiers such as
`repolens-report` remain governed by the canonical specification until a new
versioned contract explicitly replaces them.
