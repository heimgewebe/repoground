#!/usr/bin/env bash
set -Eeuo pipefail
printf '%s\n' 'repoground-publish-systemkatalog-main-if-changed is a compatibility alias; use repoground-publish-systemkatalog-main' >&2
exec "$(dirname "$0")/repoground-publish-systemkatalog-main" "$@"
